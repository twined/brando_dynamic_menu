# This file is responsible for configuring your application
# and its dependencies with the aid of the Mix.Config module.
use Mix.Config

config :brando_dynamic_menu, :otp_app, :brando_dynamic_menu
config :brando_dynamic_menu, :registry_path, Path.join(~w(priv dynamic_menu))
